import React from 'react';

const AccountView = () => {
  return (
    <div className="main-content content-center glass-card" style={{padding: '20px'}}>
      <h2>Account Details</h2>
      <p style={{marginTop: '10px'}}>AccountView: Functionality (address, balance, etc.) to be added in Prompt 4.1.</p>
      {/* Account info, send button, etc. will go here */}
    </div>
  );
};

export default AccountView;
